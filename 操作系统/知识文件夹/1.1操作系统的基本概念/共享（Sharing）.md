**共享是指系统中的资源可供内存中多个并发执行的进程共同使用**。根据资源特性与访问方式的不同，共享主要分为 **互斥共享 同时访问**两种形式。
1. 互斥共享方式
系统中的某些资源，如打印机，磁带机等，虽然可供多个进程使用，但为避免所打印或记录的结果混淆，**必须保证在任一时间段内仅有一个进程访问该资源。**
将在一段时间内只允许一个进程访问的资源称为**临界资源**。
2. 同时访问方式
允许多个进程在宏观上“同时”访问。此处的“同时”通常指逻辑上的并发访问，微观上这些进程可能通过分时交替方式访问资源，即**分时共享**。
