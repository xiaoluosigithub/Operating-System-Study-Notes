为实现对作业的连续处理，系统先将一批作业以脱机方式输入到磁带，并配备一个**监督程序**（Monitor）。在其控制下，作业能自动、顺序地逐个运行。**尽管作业成批提交，但内存中始终仅驻留一道用户程序。**

主要特征：
1. **自动性**：作业自动执行，无需人工干预
2. **顺序性**：作业按磁带上顺序依次装入内存，先装入者先完成。
3. **单道性**：内存中仅允许一道用户程序运行；监督程序每次仅从磁带调入一道作业，待其完成或异常终止后才装入下一道。

当正在运行的作业发起I/O请求时，高速CPU不得不空闲等待低速I/O操作结束，导致资源利用率低下。