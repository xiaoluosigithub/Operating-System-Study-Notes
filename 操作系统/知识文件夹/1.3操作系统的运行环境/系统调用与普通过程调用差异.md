1. **运行状态不同**：普通过程调用的调用者与被调用者运行在统一特权级；系统调用的调用者运行在用户态，被调用者运行在内核态
2. **状态转换不同**：普通过程调用无须特权级切换，可以直接跳转；系统调用则需要通过陷入指令（Trap）触发，先从用户态切换到内核态，再由内核分派至相应的处理程序
3. **返回机制不同**：系统调用执行完毕后，内核可能根据调度策略决定是否切换进程；而普通过程调用总是直接返回原调用点。
4. **嵌套调用限制不同**：系统调用的嵌套深度受到内核栈空间的限制；普通过程调用的嵌套深度通常仅受用户栈大小约束